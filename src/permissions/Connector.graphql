query create {
  SomeUserExists(filter: {id: $user_id, projects_some: {id: $input_projectId}})
}
query update($node_id: ID) {
  SomeConnectorExists(filter: {id: $node_id, project: {users_some: {id: $user_id}}})
}
query read($node_id: ID) {
  SomeConnectorExists(filter: {id: $node_id, project: {users_some: {id: $user_id}}})
}
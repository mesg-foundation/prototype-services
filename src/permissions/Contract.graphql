query create {
  SomeUserExists(filter: {id: $user_id, projects_some: {id: $input_projectId}})
}
query read($node_id: ID) {
  SomeContractExists(filter: {id: $node_id, project: {users_some: {id: $user_id}}})
}
query update($node_id: ID) {
  SomeUserExists(filter: {id: $user_id, projects_some: {contracts_some: {id: $node_id}}})
}
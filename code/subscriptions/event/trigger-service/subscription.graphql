subscription {
  Event(filter: {
    mutation_in: [CREATED]
  }) {
    updatedFields
    node {
      id
      payload
      transactionId
      blockId
      from
      to
      value
      fees
      trigger {
        id
        connector {
          ethereumContract {
            eventName
            contract {
              address
              chain
              name
            }
          }
        }
        serviceData
        service {
          endpoint
        }
      }
    }
  }
}
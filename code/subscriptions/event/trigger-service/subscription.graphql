subscription {
  Event(filter: {
    mutation_in: [CREATED]
  }) {
    updatedFields
    node {
      id
      payload
      transactionId
      blockId
      from
      to
      value
      fees
      trigger {
        id
        connector {
          ethereumContract {
            eventName
            contract {
              address
              chain
              name
            }
          }
        }
        action {
          service {
            id
            endpoint
          }
          data
        }
      }
    }
  }
}